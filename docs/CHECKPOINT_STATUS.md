# Checkpoint Status

Baseline date: 2026-08-18

Current phase: P0-03 DONE (Product Owner confirmed PASS 2026-08-21; merged to `develop` via PR #7); P0-04 DONE (Product Owner confirmed PASS 2026-08-21; merged to `develop` via PR #8, `137363b` → merge `dfddeae`); P0-05 DONE (Product Owner confirmed PASS 2026-08-22; merged to `develop` via PR #9, `a4ef4ca` → merge `75e8b0f`); P0-06 DONE (Product Owner confirmed live acceptance PASS 2026-08-24; merged to `develop` via PR #11, `042f65b` → merge `340b678`); P0-07 DONE (Product Owner confirmed live acceptance PASS 2026-08-24; merged to `develop` via PR #12, `b463742` → merge `ba4c2ba`); P0-08 DONE (merged to `develop` via PR #13, `cf2ae3e` → merge `a91c041`)

Last synced: 2026-08-25

Current checkpoint to open: **P0-09 — Acceptance, Hardening & Demo (Pha A)** (P0-08 merged to `develop`; branch `feature/p0-09-deployment-readiness` created from `develop` @ `a91c041`)

## Git branch control

| Item                   | Current value                 |
| ---------------------- | ----------------------------- |
| Branch strategy        | `main → develop → feature/**` |
| Stable/release branch  | `main`                        |
| Integration branch     | `develop`                     |
| Expected active branch | `feature/p0-09-deployment-readiness` (created from `develop`) |
| Actual active branch   | `feature/p0-09-deployment-readiness` (confirmed 2026-08-25: `git merge-base develop HEAD` = `a91c041`, i.e. branched from `develop` HEAD; P0-09 work in progress) |
| Base commit            | `a91c041` (develop HEAD, includes merged P0-03 → P0-08) |
| Merge target           | `develop`                     |
| Current merge status   | P0-03 MERGED to `develop` (PR #7: `c06cd02` → `37c0e4d`); P0-04 MERGED to `develop` (PR #8: `137363b` → `dfddeae`); P0-05 MERGED to `develop` (PR #9: `a4ef4ca` "feat: add AI host" → merge `75e8b0f`); P0-06 MERGED to `develop` (PR #11: `042f65b` → merge `340b678`); P0-07 MERGED to `develop` (PR #12: `b463742` "feat: add generate email tool" → merge `ba4c2ba`); P0-08 MERGED to `develop` (PR #13: `cf2ae3e` "feat: add minimal web ui with traces and sources" → merge `a91c041`). `develop` has **not** been merged into `main` — `main` is still at `afa47fd` (docs baseline), tracked as a P0-09 known limitation. |

## 1. Status board

| Checkpoint                           | Status      | Reviewer verdict | Evidence summary                                                                                                                                                                 | Next action                       |
| ------------------------------------ | ----------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- |
| P0-00 Documentation Baseline         | DONE        | PASS             | 16-file Markdown planning kit cross-checked                                                                                                                                      | Open P0-01 planning               |
| P0-01 Repository & Solution Scaffold | DONE        | PASS             | restore PASS; build PASS with 0 warnings/errors; tests 3/3 PASS using Microsoft.Testing.Platform; three Kestrel health endpoints returned HTTP 200; secret hygiene PASS. | Open P0-02 planning                |
| P0-02 Synthetic Data & Mock CRM API  | DONE        | PASS             | Dataset 12 customers / 26 interactions (seed 20260818), deterministic + golden-tested against checked-in JSON; restore/build PASS with 0 warnings/errors; tests 50/50 PASS; customer lookup/search (incl. 409 AMBIGUOUS_MATCH data-envelope contract) and interaction endpoints verified by test + manual curl; MockCrmGateway full error mapping and ValidateOnStart fail-fast verified; README `.env` wording corrected in final audit. | Open P0-03 planning               |
| P0-03 Gemini Embedding & Chroma RAG  | DONE        | PASS             | Implementation merged to `develop` via PR #7 (`c06cd02` "feat: add RAG with Chroma" → merge `37c0e4d`): embedding client, Chroma HTTP adapter, idempotent ingestion, retriever, unit tests + opt-in `LiveRagAcceptanceTests`. 2026-08-21 offline: `dotnet restore` PASS; `dotnet build CrmCopilot.slnx --no-restore` PASS, 0 warnings/0 errors; `dotnet test` 121/122 passed, 1 skipped (`LiveRagAcceptanceTests`, no live creds — expected). 2026-08-21 **mandatory live acceptance run** (real Gemini `gemini-embedding-001` + Chroma `1.5.9` local container, isolated collection `crm-copilot-knowledge-livetest`, dev collection `crm-copilot-knowledge` untouched): heartbeat OK; ingest run 1 (fresh collection) → 14 embedded, count=14; ingest run 2 → 0 embedded/14 unchanged (idempotent confirmed); query embedding L2 norm=1.000000; canonical query top-3 = `PRD-SAV-006M` (distance 0.472959, rank 1), `PRD-SAV-012M`, `TPL-EMAIL-MATURITY-01`; `dotnet test --filter-class LiveRagAcceptanceTests` 1/1 PASS. No API key or payload logged. Product Owner confirmed PASS 2026-08-21. | Open P0-04 planning               |
| P0-04 MCP Server Core Tools          | DONE        | PASS             | Implementation merged to `develop` via PR #8 (`137363b` "feat: add core MCP tools" → merge `dfddeae`): official SDK `ModelContextProtocol.AspNetCore` 2.2.0, Streamable HTTP (`HttpServerSessionMode.Stateless`), `/mcp` endpoint; 3 tools `get_customer`/`get_interactions`/`search_product_knowledge` via existing `ICrmGateway`/`IKnowledgeRetriever` (unchanged); shared tool-result envelope in `Contracts/Mcp/`. `dotnet build` PASS, 0 warnings/0 errors; `dotnet test` 155/156 passed, 1 skipped (`LiveRagAcceptanceTests`, unrelated opt-in); `git diff --check` PASS (exit 0). **Live MCP verification 2026-08-21** (real McpServer + MockCrmApi + Chroma, real `GEMINI_API_KEY` held only in Product Owner's own terminal, never read/printed/logged by Claude): `tools/list` → exactly 3 tools with correct schemas; all 3 canonical calls PASS live — `get_customer CUS-0001`, `get_interactions CUS-0001 limit=5`, and `search_product_knowledge` canonical query topK=3 using real Gemini embedding + Chroma (`crm-copilot-knowledge-livetest` collection); `PRD-SAV-006M` ranked first in top-3 (distance 0.47295862, lower-is-better, matches P0-03's recorded 0.472959), no fabricated `title`/`score`; invalid `topK=6` → `INVALID_ARGUMENT`; no API key or raw exception leaked in any response. Product Owner confirmed PASS 2026-08-21. | Open P0-05 planning               |
| P0-05 AI Host + MCP Client           | DONE        | PASS             | Implementation merged to `develop` via PR #9 (`a4ef4ca` "feat: add AI host" → merge `75e8b0f`), branch pushed to GitHub. Implementation on `feature/p0-05-ai-host` (branched from `develop` @ `dfddeae`, plan Revision 3 approved 2026-08-22): Gemini `gemini-3.5-flash-lite` tool-calling loop bounded to 3 MCP calls/turn (D-loop), async `IMcpClientProvider` (no sync-over-async), tool allowlist = intersection of approved names and MCP discovery (D5), pre-Gemini PII/CRM-intent input gate (D7), no-PII-to-Gemini data minimization (D1), `POST /api/chat`. `dotnet restore` PASS; `dotnet build CrmCopilot.slnx --no-restore` PASS, 0 warnings/0 errors; `dotnet test` 206/207 passed, 1 skipped (`LiveRagAcceptanceTests`, unrelated opt-in). **Live acceptance finding + fix (2026-08-22, before final live PASS):** first live `get_customer` run returned HTTP 400 `DUPLICATE_TOOL_CALL` — root cause: the minimized tool-result payload sent back to Gemini (`{status,sourceIds}` only) was insufficient for the model to recognize the call as complete, so it re-requested the identical call; fixed by adding the non-PII `customerId`/`interactionCount` fields to the minimized payload and one system-instruction line. Same live-gate run also surfaced that a controlled error response could carry the turn's already-fetched `CustomerDto` (PII) in `Data` — fixed: `Data` is now always `null` on any `Status=Error`/`NotFound` outcome; new/updated tests added (`ChatEndpointTests`, `McpToolResultParserTests`) proving both the fix and that `DUPLICATE_TOOL_CALL` still fires with exactly one real MCP call. **Mandatory live acceptance gate PASS 2026-08-22** (real Gemini `gemini-3.5-flash-lite` + real McpServer + MockCrmApi + Chroma with the default `crm-copilot-knowledge` collection ingested — resolves Blocker B-01; real `GEMINI_API_KEY` held only in Product Owner's own terminal, never read/printed/logged by Claude), filtered evidence: `get_customer` → HTTP 200, `status:success`, non-empty reply, `sourceIds=[crm:customer:CUS-0001]`, ToolTrace 1 entry (`get_customer`/success), exactly one MCP call, `errorCode:null`; `get_interactions` → HTTP 200, `status:success`, non-empty reply, `interactionCount:3`, `sourceIds=[crm:interaction:INT-0001..003]`, ToolTrace 1 entry, exactly one MCP call, `errorCode:null`; `search_product_knowledge` → HTTP 200, `status:success`, non-empty reply, `sourceIds=[kb:product:PRD-SAV-006M, PRD-SAV-012M, PRD-LOAN-PERSONAL-01]` with canonical `PRD-SAV-006M` ranked first, ToolTrace 1 entry, exactly one MCP call, `errorCode:null`. No PII is sent to Gemini (D1's data-minimized `FunctionResponse` payloads); no accumulated PII appears in any controlled-error response (`Data=null` on `Status=Error`/`NotFound`, verified by test); the filtered evidence recorded above contains no PII, raw DTO, or API key. On a **successful** turn, `ChatResponse.Data.Customer`/`Data.Interactions` still legitimately carries the full CRM DTO exactly as the approved contract specifies (docs/07 §4's trusted-local-path design, for the caller/UI, never sent to Gemini) — that is not a PII leak and is not being claimed as absent. Product Owner confirmed live gate PASS 2026-08-22; formal checkpoint PASS confirmed 2026-08-22. | Open P0-06 planning |
| P0-06 Conversation State             | DONE        | PASS             | Implementation merged to `develop` via PR #11 (`042f65b` "feat: add P0-06 conversation state" → merge `340b678`, 2026-08-24): `IConversationStateStore`/`InMemoryConversationStateStore`, `sessionId` bắt buộc trên `/api/chat`, tái sử dụng `CurrentCustomerId` cho "khách hàng này", endpoint `DELETE /api/chat/sessions/{sessionId}`, `ConversationMessageSanitizer` (PII defense-in-depth). Offline: branch `feature/p0-07-email-pii` @ `a4deccf`, build succeeded, `dotnet test` 232/233 passed, 1 skipped (`LiveRagAcceptanceTests`, unrelated opt-in), 0 failed. Health checks: Chroma heartbeat HTTP 200, MockCrmApi/McpServer/Web `/health` all HTTP 200. **Live acceptance run 2026-08-24** (real Gemini + Chroma + MockCrmApi + Web, `GEMINI_API_KEY` held only in Product Owner's own terminal, never read/printed/logged by Claude): Session 1 (`241798cd-1697-4f95-87f2-8ae35a1d1cb6`) turn 1 `"Tìm khách hàng CUS-0001"` → `status:success`, `get_customer` success, resolved `CUS-0001`, `error:null`; turn 2 same session `"Xem các tương tác gần đây của khách hàng này"` (message không chứa `CUS-0001`) → `status:success`, `get_interactions` success, trả về `INT-0001/002/003` cho `CUS-0001` — chứng minh `CurrentCustomerId` được resolve từ session state. Session 2 (`8ad7e9e4-137e-4945-9ae8-32cf5ce9f822`), phiên mới không có lượt trước, cùng câu "khách hàng này" → HTTP 400, `status:error`, `error.code:CUSTOMER_ID_REQUIRED`, `retryable:false`, `toolTrace` rỗng — chứng minh session isolation và hành vi yêu cầu làm rõ. Stored-state PII: `ConversationStateStoreTests`/`ConversationMessageSanitizerTests` PASS trong full offline suite; không API key nào xuất hiện trong response quan sát được; raw CRM PII vẫn có thể xuất hiện trong `data.customer` theo đúng contract hiện có (P0-06 guarantee áp dụng cho stored message/state, không áp dụng cho mọi CRM response). Product Owner confirmed PASS 2026-08-24. | Checkpoint đóng; P0-07 (RAG Email Draft + PII) là checkpoint tiếp theo |
| P0-07 RAG Email Draft + PII          | DONE        | PASS             | Merged to `develop` via PR #12 (`b463742` → merge `ba4c2ba`). New MCP tool `generate_email` (`ReadOnly=true`, `Destructive=false`): `EmailTools`, `GeminiEmailDraftGenerator`, `PiiMasker`, `RawEmailDraftModel`, `EmailGenerationException`; `PiiPatterns` relocated to `CrmCopilot.Contracts.Pii` (public, shared by `CrmCopilot.Web` and `CrmCopilot.McpServer`). Two independent code-review rounds found issues, all fixed and re-verified before this PASS: Revision 2 (12 points) + an Amendment (4 points) during planning, then a post-implementation review finding 2 required fixes (null model-collection handling routed through the existing one-retry path instead of `INTERNAL_ERROR`; `HashForAudit` null-safety so a null `customerId` cannot throw past an already-computed `INVALID_ARGUMENT`) + 1 robustness fix (log-hygiene test strengthened to assert every captured `Exception` is null and to scan for `Customer.FullName`) — all applied, re-verified below. **Offline, after final review fixes**: `dotnet build` 0 Warning(s)/0 Error(s); full suite 324 total, 322 succeeded, 0 failed, 2 skipped (both opt-in live tests — pre-existing `LiveRagAcceptanceTests` and new `LiveEmailGenerationAcceptanceTests` — correctly Skipped, not Passed, without live credentials); `EmailToolsTests` 58/58; `EmailToolsLogHygieneTests` 1/1; `GeminiEmailDraftGeneratorPromptTests` 9/9; `McpToolProtocolTests` 13/13; `git diff --check` exit 0. **Mandatory live acceptance run 2026-08-24** (real Gemini `gemini-3.5-flash-lite`/`gemini-embedding-001` + real Chroma + real MockCrmApi; `GEMINI_API_KEY` held only in Product Owner's own terminal, never read/printed/logged by Claude). Environment: Chroma heartbeat HTTP 200; MockCrmApi `/health` HTTP 200 at `localhost:5100`; McpServer `/health` HTTP 200 at `localhost:5090`. Automated opt-in `LiveEmailGenerationAcceptanceTests`: total 1, succeeded 1, failed 0, skipped 0, duration 11.201s, against the isolated `crm-copilot-knowledge-livetest` collection. Default `crm-copilot-knowledge` collection idempotent-ingest re-run: 14 documents, 0 embedded/14 unchanged, collection count after ingest=14, embedding model `gemini-embedding-001`, dimension 768, metric `l2`, duration 3793ms. `tools/list` → exactly 4 tools: `generate_email`, `get_customer`, `get_interactions`, `search_product_knowledge`. **Canonical `generate_email` call** (`customerId:"CUS-0001"`, `objective:"Follow-up nhu cầu gửi tiết kiệm kỳ hạn 6 tháng"`, `tone:"professional_warm"`, `productCode:null`) → `status:success`, `error:null`, `sourceIds=[kb:product:PRD-SAV-006M, kb:email-template:TPL-EMAIL-MATURITY-01, crm:interaction:INT-0001]`, `suggestedProductCode:"PRD-SAV-006M"`, `requiresHumanApproval:true`; body had the `{{CUSTOMER_NAME}}` placeholder correctly restored to the synthetic name "Nguyễn Minh Anh", and the raw placeholder token did not appear in the final body; no raw email/phone/account reference/API key/raw model output/raw exception observed anywhere in the response. Audit log (derived-safe fields only, per `EmailTools`' own logging design) recorded: `status:success`, `durationMs:5036`, generation model `gemini-3.5-flash-lite`, `customerIdHash` (hashed, never the raw ID), `maskedFieldTypes=name,email,phone,accountReference`, `warningCount:0`, `errorCode:(none)`. **Negative control — invalid tone** (`tone:"friendly"`) → `status:error`, `sourceIds:[]`, `data:null`, `error.code:INVALID_ARGUMENT`, `error.retryable:false`, safe validation message, no raw exception or sensitive data. **Negative control — nonexistent customer** (`customerId:"CUS-9999"`, `tone:"professional_warm"`) → `status:not_found`, `sourceIds:[]`, `data:null`, `error.code:NOT_FOUND`, `error.retryable:false`, message "Không tìm thấy khách hàng phù hợp.", no raw exception or customer PII. `EmailTools`' own audit logging contained only derived/safe fields — never the generated email body, customer name, email, phone, account reference, API key, or SDK exception. **Known non-blocking hardening note** (not a P0-07 acceptance failure): the ASP.NET Core `HttpClient` framework's own informational logging (not `EmailTools`' application-level audit logging) displayed the synthetic, low-sensitivity customer ID `CUS-9999` as part of a request URL during the nonexistent-customer negative control — `CUS-9999` is a synthetic non-secret identifier, not raw customer PII, and this is framework-level HTTP logging outside this checkpoint's own code; tracked here as a backlog hardening item (e.g. reducing `HttpClient` log verbosity in a future checkpoint), not as a gap in P0-07's own PII-masking guarantees. Product Owner confirmed live acceptance PASS 2026-08-24. | Checkpoint đóng; P0-08 (Web UI + Trace + Sources) là checkpoint tiếp theo |
| P0-08 Web UI + Trace + Sources       | DONE (merged) | **chưa ghi nhận trong file này** — xem B-04 | Row này được **dựng lại từ git history ngày 2026-08-25** trong lúc đồng bộ tài liệu của P0-09; không phải bản ghi tại thời điểm review. Những gì git chứng minh được: merged vào `develop` qua PR #13 (`cf2ae3e` "feat: add minimal web ui with traces and sources" → merge `a91c041`); diff 20 files, +1579/−106. Deliverable quan sát được trong repo: `Pages/Index.cshtml` (customer card, candidate list, interaction list, draft panel, source chips, tool-trace accordion, nút "New conversation"), `wwwroot/js/app.js` (406 dòng, **không dùng `innerHTML` với giá trị nội suy** — chỉ `textContent`, xem `app.js:53`; nút Send/Reset/input bị disable khi busy, `app.js:115-117`), `wwwroot/css/app.css`; phía backend thêm "terminal tool rule" (một `get_customer`/`get_interactions` thành công kết thúc lượt ngay, reply do Host render deterministic) và `EmailDraftDto` cho UI. Test đi kèm: `ChatEndpointTests` (+525 dòng), `EmailToolsTests` (+92), `GeminiEmailDraftGeneratorPromptTests` (+37), `McpToolResultParserTests` (+17). Offline suite tại `a91c041` xanh (đo lại 2026-08-25: 350 total/348 passed/0 failed/2 skipped, cả 2 skip là live opt-in). **Chưa có** live-acceptance run hay Product Owner PASS verdict nào được ghi trong file này cho P0-08 — đây là khoảng trống quy trình, không phải khẳng định rằng nó đã fail. | Product Owner xác nhận verdict P0-08 (đóng B-04). Không chặn P0-09 vì P0-09 tự chạy lại acceptance đầu-cuối. |
| P0-09 Acceptance & Demo (Pha A)      | IN PROGRESS | —                | Branch `feature/p0-09-deployment-readiness` từ `develop` @ `a91c041`. Đã xong: đồng bộ tài liệu (row này + README + docs/10 §18); bộ 8 scenario T01–T08 (`tests/CrmCopilot.Tests/Acceptance/`) với runner xác định + report sinh từ cùng một nguồn kết quả; hardening B-03. Offline evidence 2026-08-25 — **C1** `dotnet restore` + `dotnet build CrmCopilot.slnx --no-restore` PASS, **0 Warning / 0 Error**; **C2** `dotnet test` **352 total / 349 passed / 0 failed / 3 skipped**, tập skip đúng bằng 3 live opt-in đã liệt kê (`LiveRagAcceptanceTests`, `LiveEmailGenerationAcceptanceTests`, `LiveAcceptanceScenarioTests`); **C3** 0 scenario `Outcome == Error`; **C4** `AcceptanceScenarioTests` **ScenarioAccuracy 8/8** (ngưỡng khóa 7/8), 75 check, quality target 8/8 MET; **C7** secret scan phủ committed + staged + unstaged + untracked (corpus 1633 dòng, gồm cả 14 file mới chưa commit) — 0 khớp `AIza[…]{35}`, 0 khớp gán biến nhạy cảm, 0 file bí mật trong index, `git diff --check` exit 0; **C9** tài liệu đã đồng bộ. **Chưa đạt**: **C5** live gate (3 test đang Skipped), **C6** 3 browser demo run, **C8** runtime-log PII scan. Theo §9.1 verdict hiện tại là **PARTIAL** — live gate chưa chạy thì không được kết luận PASS. | Product Owner khởi động lại 3 service (đang DOWN; Chroma vẫn Up) kèm `Tee-Object` ra `logs/`, chạy live gate + 3 demo run + runtime-log PII scan, rồi chấm verdict theo §9.1 |

Allowed status values: `NOT STARTED`, `PLANNING`, `APPROVED`, `IN PROGRESS`, `REVIEW`, `REWORK`, `BLOCKED`, `DONE`.

## 2. Frozen baseline

| Item                 | Value actually implemented                                                    | Evidence/version date                   |
| -------------------- | ----------------------------------------------------------------------------- | --------------------------------------- |
| .NET SDK/target      | `10.0.400` (`net10.0`), pinned qua `global.json` (`rollForward: latestPatch`) | 2026-08-19                              |
| MCP packages         | `ModelContextProtocol` + `ModelContextProtocol.AspNetCore` `2.2.0` (exact pin), verified against NuGet v3-flatcontainer raw index | Verified 2026-08-21 (P0-04) |
| MCP transport        | Streamable HTTP                                                               | Decision baseline                       |
| Gemini chat          | `gemini-3.5-flash-lite`                                                       | Verified live 2026-08-22 (P0-05 mandatory live gate, real API calls) |
| Gemini embedding     | `gemini-embedding-001`                                                        | Verified live 2026-08-21 (real API call) |
| Embedding dimension  | 768 + L2 normalized (query norm=1.000000 observed live)                      | Verified live 2026-08-21                |
| Chroma version/image | `chromadb/chroma:1.5.9`, distance metric `l2`, HTTP client (no official .NET package) | Verified live 2026-08-21          |
| Dataset version/hash | seed=20260818, 12 customers / 26 interactions; SHA-256 customers.json=`14c500f9…6c4b2e9d8`, interactions.json=`52af5586…3a0a52da558` | 2026-08-19 |

## 3. Review log

| Date       | Checkpoint | Artifact reviewed      | Verdict | Required follow-up            |
| ---------- | ---------- | ---------------------- | ------- | ----------------------------- |
| 2026-08-18 | P0-00      | Documentation baseline | PASS    | Begin P0-01 plan; no code yet |
| 2026-08-19 | P0-02      | Synthetic dataset + Mock CRM API implementation, plus final audit corrections (409 contract, MockCrmGateway error mapping, ValidateOnStart fail-fast, README `.env` wording) | PASS | Open P0-03 planning |
| 2026-08-21 | P0-03      | Gemini embedding + Chroma RAG implementation (`feature/p0-03-rag-chroma`, merged to `develop` via PR #7); offline test suite + mandatory live acceptance run (real Gemini/Chroma) | PASS | Open P0-04 planning |
| 2026-08-21 | P0-04      | MCP Server Core Tools implementation (`feature/p0-04-mcp-server-tools`, merged to `develop` via PR #8); offline test suite + live MCP verification (real Gemini/Chroma, all 3 tools, canonical + invalid-argument cases) | PASS | Open P0-05 planning |
| 2026-08-22 | P0-05      | AI Host + MCP Client implementation (`feature/p0-05-ai-host`, merged to `develop` via PR #9, `a4ef4ca` → merge `75e8b0f`); offline test suite (206/207 passed, 1 unrelated opt-in skip) + mandatory live acceptance gate (real Gemini `gemini-3.5-flash-lite`, all 3 tools, single-MCP-call-per-turn, no PII to Gemini/error paths); one live-gate finding (`DUPLICATE_TOOL_CALL` due to an under-informative tool-result payload, plus a PII leak in controlled-error `Data`) found and fixed before final PASS | PASS | Open P0-06 planning |
| 2026-08-24 | P0-06      | Conversation State implementation (`feature/p0-06-conversation-state`, merged to `develop` via PR #11, `042f65b` → merge `340b678`), reviewed retroactively (merged 2026-08-24 without prior live-acceptance evidence — see resolved blocker B-02); offline test suite 232/233 passed, 1 unrelated opt-in skip, 0 failed + mandatory live acceptance run (real Gemini + Chroma + MockCrmApi + Web) proving `CurrentCustomerId` resolution across turns, session isolation, and `CUSTOMER_ID_REQUIRED` clarification behavior — full evidence in §1's P0-06 row | PASS | Open P0-07 planning |
| 2026-08-25 | P0-08      | Web UI + Trace + Sources — **không có review nào được ghi tại thời điểm merge**; dòng này chỉ ghi nhận việc phát hiện khoảng trống đó khi đồng bộ tài liệu ở P0-09 (chi tiết những gì git chứng minh được nằm ở row P0-08 của §1) | **chưa có verdict** | Product Owner xác nhận verdict P0-08 và đóng B-04 |
| 2026-08-24 | P0-07      | RAG Email Draft + PII (`generate_email` MCP tool) implementation on `feature/p0-07-email-pii` (branched from `develop` @ `340b678`, sau đó merged qua PR #12); plan Revision 2 (12 points) + Amendment (4 points) approved before implementation; independent post-implementation code review found 2 required fixes + 1 robustness fix, all applied and re-verified; offline test suite 324 total/322 succeeded/0 failed/2 skipped (`EmailToolsTests` 58/58, `EmailToolsLogHygieneTests` 1/1, `GeminiEmailDraftGeneratorPromptTests` 9/9, `McpToolProtocolTests` 13/13), `git diff --check` exit 0 + mandatory live acceptance run (real Gemini/Chroma/MockCrmApi): 4-tool `tools/list` discovery, canonical `generate_email` call grounded in retrieved product+template+interaction evidence with correct placeholder restore and server-forced `requiresHumanApproval=true`, both negative controls (invalid tone, nonexistent customer) verified, `EmailTools` audit logging confirmed to carry only derived-safe fields — full evidence in §1's P0-07 row | PASS | Open P0-08 planning |

## 4. Blocker log

| ID  | Opened | Checkpoint | Blocker           | Owner | Status/decision |
| --- | ------ | ---------- | ----------------- | ----- | --------------- |
| B-01 | 2026-08-21 | P0-04 (found), targets P0-05 | Default Chroma collection `crm-copilot-knowledge` has 0 records — never ingested (only the isolated `crm-copilot-knowledge-livetest` collection, from P0-03's mandatory live acceptance run, is populated). Does **not** block P0-04 (its live verification explicitly used `crm-copilot-knowledge-livetest` via `CHROMA_COLLECTION_NAME`). | Product Owner | **Resolved 2026-08-22** — default collection `crm-copilot-knowledge` ingested (`--ingest-knowledge`, `CHROMA_COLLECTION_NAME` left unset) as part of P0-05's mandatory live acceptance gate; live `search_product_knowledge` call against it returned canonical `PRD-SAV-006M` ranked first. `crm-copilot-knowledge-livetest` was not touched/reused. |
| B-04 | 2026-08-25 | P0-08 (phát hiện khi đồng bộ tài liệu ở P0-09) | P0-08 được merge vào `develop` (PR #13, `cf2ae3e` → `a91c041`) mà file này chưa từng có row status, evidence hay Product Owner PASS verdict cho nó — §1 vẫn ghi `NOT STARTED`, §3 không có dòng review nào. Cùng dạng khoảng trống quy trình với B-02 (P0-06). Không chặn P0-09: bộ 8 scenario của P0-09 chạy lại toàn bộ luồng đầu-cuối, gồm cả UI-facing contract mà P0-08 thêm vào. | Product Owner | **Open — đường đóng đã chốt 2026-08-25.** Product Owner quyết định: **không** ghi ngược P0-08 là PASS khi không có historical live evidence. Trạng thái giữ nguyên là "implementation merged qua PR #13, historical acceptance evidence not recorded". Sau khi P0-09 đạt **C5** (cả 3 live gate PASS) **và C6** (3 browser demo run PASS liên tiếp), dùng chính regression evidence của P0-09 để đóng B-04 — vì bộ 8 scenario và luồng demo của P0-09 chạy lại toàn bộ UI-facing contract mà P0-08 đưa vào. Khi đóng, phải ghi rõ **nguồn evidence mới** (ngày chạy, lệnh, ScenarioAccuracy, kết quả 3 run) và ghi rõ rằng đây là evidence hồi quy của P0-09, **không phải** bản ghi review gốc của P0-08. |
| B-03 | 2026-08-25 | P0-07 (phát hiện), xử lý ở P0-09 | Logging *của framework* `System.Net.Http.HttpClient` (không phải audit log của `EmailTools`) in URL request ra console ở mức Information, nên mã khách hàng xuất hiện dưới dạng `.../api/customers/CUS-9999`. Ghi nhận trong evidence P0-07 như "known non-blocking hardening note". | Claude / Product Owner | **Fixed 2026-08-25 (chờ verify runtime)** — thêm `"System.Net.Http.HttpClient": "Warning"` vào `Logging:LogLevel` trong `appsettings.json` của `CrmCopilot.McpServer` và `CrmCopilot.Web`. Không đụng code. Đóng blocker sau khi §7.2 của plan xác nhận `logs/mcpserver.log` không còn khớp `/api/customers/CUS-\d{4}`. |
| B-02 | 2026-08-24 | P0-06 | P0-06 implementation (PR #11, `042f65b` → `340b678`) được merge vào `develop` mà chưa có live-acceptance run hay Product Owner PASS verdict ghi trong file này (§1/§3), khác với P0-02–P0-05. | Product Owner | **Resolved 2026-08-24** — mandatory live acceptance run thực hiện trên `feature/p0-07-email-pii` @ `a4deccf` (real Gemini + Chroma + MockCrmApi + Web): Session 1 (`241798cd-...`) chứng minh `CurrentCustomerId` được resolve qua 2 lượt cùng session; Session 2 (`8ad7e9e4-...`) chứng minh session isolation + `CUSTOMER_ID_REQUIRED` clarification; offline suite 232/233 passed/1 skipped/0 failed gồm `ConversationStateStoreTests`/`ConversationMessageSanitizerTests`. Đầy đủ evidence tại §1 P0-06 row và §3 review log 2026-08-24. Product Owner confirmed PASS 2026-08-24. |

## 5. Checkpoint evidence template

```markdown
### P0-XX Review Evidence — YYYY-MM-DD

- Approved plan reference:
- Changed files:
- Packages/config changes:
- Commands actually run:
- Automated test result:
- Manual verification:
- Acceptance criteria mapping:
- Security/PII check:
- Known limitations:
- Git diff/status:
- Claude report:
- ChatGPT verdict:
- Required follow-up:
- Next checkpoint allowed: yes/no
```

## 6. Cập nhật file này

- Claude đề xuất update trong completion report nhưng không tự đánh dấu `DONE` trước review.
- ChatGPT đưa verdict; Product Owner/Claude cập nhật verdict và evidence sau khi được xác nhận.
- Không xóa lịch sử review/blocker; thêm dòng mới.
- Mọi version/model/dataset hash thực tế phải điền khi được triển khai.
